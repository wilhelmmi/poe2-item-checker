services:
  app:
    build: .
    ports: ["8080:8080"]
    environment:
      DATABASE_URL: sqlite:////data/app.db
    volumes:
      - poe2-data:/data
    restart: unless-stopped
volumes:
  poe2-data:

